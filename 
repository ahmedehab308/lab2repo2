remove  
rm -rf thename

remove 
git remote remove origin


create an annoted 
git tag -a v1.4 -m


delete tag
git tag -d tagname

tag remotly
git push --delete origin tagname


rebase
reapply commits from one branch onto another
![Alt text](img.png)
